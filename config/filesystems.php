<?php

return [

    'default' => env('FILESYSTEM_DISK', 'local'),

    'disks' => [

        'local' => [
            'driver' => 'local',
            'root' => storage_path('app/private'),
            'serve' => true,
            'throw' => false,
        ],

        'public' => [
            'driver' => 'local',
            'root' => storage_path('app/public'),
            'url' => env('APP_URL').'/storage',
            'visibility' => 'public',
            'throw' => false,
        ],

        // Encrypted PII document vault (§6.1 / §9). Use a private bucket in prod.
        'vault' => [
            'driver' => 's3',
            'key' => env('VAULT_S3_KEY'),
            'secret' => env('VAULT_S3_SECRET'),
            'region' => env('VAULT_S3_REGION'),
            'bucket' => env('VAULT_S3_BUCKET'),
            'endpoint' => env('VAULT_S3_ENDPOINT'),
            'use_path_style_endpoint' => env('VAULT_S3_PATH_STYLE', false),
            'visibility' => 'private',
            'throw' => false,
        ],

        's3' => [
            'driver' => 's3',
            'key' => env('AWS_ACCESS_KEY_ID'),
            'secret' => env('AWS_SECRET_ACCESS_KEY'),
            'region' => env('AWS_DEFAULT_REGION'),
            'bucket' => env('AWS_BUCKET'),
            'url' => env('AWS_URL'),
            'endpoint' => env('AWS_ENDPOINT'),
            'use_path_style_endpoint' => env('AWS_USE_PATH_STYLE_ENDPOINT', false),
            'throw' => false,
        ],

    ],

    'links' => [
        public_path('storage') => storage_path('app/public'),
    ],

];
